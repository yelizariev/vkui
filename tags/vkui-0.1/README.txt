For coders:
1) use utf8 encoding
2) use linux newline symbol "\n" instead windows "\r\n"
3) use tab symbold "\t" instead spaces for indent
4) открывающую фигурную скопку функции ставим в конце строки описания функции (а не на новой строке). Пример:
public function draw(): void{
	...
}
